<UserControl x:Class="LabelPrinter.Controls.ImagePrintableControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Height="120" Width="300">
    <Border BorderBrush="Gray" BorderThickness="1" Padding="4" Margin="2">
        <StackPanel>
            <TextBlock Text="Image" FontWeight="Bold" />
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Path:" Width="50" VerticalAlignment="Center" />
                <TextBox x:Name="PathTextBox" Text="{Binding ImagePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="160" />
                <Button Content="Browse..." Click="BrowseBtn_Click" Margin="6,0,0,0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="X:" Width="20" />
                <TextBox Text="{Binding X, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="50" />
                <TextBlock Text="Y:" Width="20" />
                <TextBox Text="{Binding Y, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="50" />
                <TextBlock Text="W:" Width="20" />
                <TextBox Text="{Binding Width, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="50" />
                <TextBlock Text="H:" Width="20" />
                <TextBox Text="{Binding Height, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="50" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Rotation:" Width="60" />
                <TextBox Text="{Binding Rotation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="50" />
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>